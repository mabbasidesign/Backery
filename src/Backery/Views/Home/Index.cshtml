@model HomeViewModel


@await Html.PartialAsync("Carousel")

<h2>Bread of the week</h2>
<h4>Our weekly selection - just for you!</h4>

<div class="row">

    @foreach (var pie in Model.BreadOfTheWeek)
    {
        @Html.Partial("BreadOverViewSummery", pie)
    }

</div>